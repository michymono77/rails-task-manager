<div class="container">
  <h1>Task name: <%= @task.title %></h1>
  <h2>-Details of the task-</h1>
    <p><%= @detail%></p>

  <%= link_to 'back', tasks_path %>
  <%= link_to 'edit', edit_task_path(@task) %>
  <%= link_to 'delete', task_path(@task), method: :delete, data: { confirm: "Are you sure to deliete #{@task.title}? "} %>
  <%= link_to task_path(@task), method: :delete, data: { confirm: "Do you want to delete #{@task.title}" } do %>
          <i class="fas fa-times"></i>
        <% end %>
</div>
